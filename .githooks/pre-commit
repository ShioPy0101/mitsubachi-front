#!/usr/bin/env bash
set -euo pipefail

mapfile -d "" staged_files < <(git diff --cached --name-only --diff-filter=ACMR -z)

if ((${#staged_files[@]} == 0)); then
  exit 0
fi

npx --no-install prettier --write --ignore-unknown "${staged_files[@]}"
git add -- "${staged_files[@]}"
